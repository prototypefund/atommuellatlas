<div class="welcome-screen" style="background-image: url('{{ image.hero.src }}')">
    <!--div class="welcome-screen-hero">
    </div-->
    <div class="welcome-screen-top">
        <div class="welcome-screen-top-logo">
            {{ data.icon.logo }}
        </div>

        <div class="welcome-screen-top-title">
            <h1>{{ data.title }}</h1>
        </div>

        <div class="welcome-screen-top-about">
            <a>{{ data.about.label }}</a>
        </div>
    </div>
    <div class="welcome-screen-bottom">
        <p>{{ data.welcomeText }}</p>
        <nav></nav>
    </div>
</div>